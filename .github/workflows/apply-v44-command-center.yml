name: Apply V44 Command Center
on:
  push:
    branches: [feature/command-center-v44]
    paths:
      - tools/patch_v44_command_center.py
      - tools/patch_v44_activity_detail.py
      - .github/workflows/apply-v44-command-center.yml
  workflow_dispatch:
permissions:
  contents: write
jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 6
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/command-center-v44
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply V44 patches
        shell: bash
        run: |
          set -euo pipefail
          python -m py_compile tools/patch_v44_command_center.py tools/patch_v44_activity_detail.py
          python tools/patch_v44_command_center.py
          python tools/patch_v44_activity_detail.py
          grep -q 'V44_QA_BOOTSTRAP' index.html
          grep -q 'v44-observability.js?v=44.0' index.html
          grep -q 'v44-observability.js?v=44.0' customer.html
          grep -q 'v44-admin-bridge.js?v=44.0' admin-dashboard.html
          grep -q 'v44-admin-bridge.js?v=44.0' security-center.html
          grep -q 'category_select' v44-observability.js
          grep -q 'button_click' command-center.html
      - name: Commit generated V44 files
        shell: bash
        run: |
          set -euo pipefail
          if git diff --quiet -- index.html customer.html admin-dashboard.html security-center.html v44-observability.js command-center.html; then
            echo 'already applied'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html customer.html admin-dashboard.html security-center.html v44-observability.js command-center.html
          git commit -m 'V44: generate command center, telemetry, permissions, and QA isolation'
          git push origin HEAD:feature/command-center-v44
