(()=>{
'use strict';
const VERSION='54.0';
try{
const DAY=86400000;
function periodDays54(){return S.range==='all'?Math.max(1,Math.ceil(((evs()[0]&&new Date(evs()[0].timestamp).getTime())-(evs().at(-1)&&new Date(evs().at(-1).timestamp).getTime()))/DAY)||30):Number(S.range)}
function previous54(){if(S.range==='all')return null;const days=Number(S.range),now=Date.now(),from=now-days*2*DAY,to=now-days*DAY,t={sales:0,value:0,j:0,r:0,inbound:0};for(const e of S.data?.events||[]){const tm=new Date(e.timestamp).getTime();if(tm<from||tm>=to)continue;for(const c of e.changes||[]){if(S.branch!=='all'&&c.branch!==S.branch)continue;const q=saleQty(c);t.sales+=q;t.value+=q*Number(c.price||0);if(c.branch==='jeddah')t.j+=q;else t.r+=q;if(c.delta>0)t.inbound+=Number(c.delta)}}return t}
const pct54=(a,b)=>!b?(a?100:0):(a-b)/Math.abs(b)*100;
const badge54=(a,b)=>{if(b===null||b===undefined)return'<span class="v54-trend flat">الفترة الحالية</span>';const p=pct54(a,b),c=Math.abs(p)<.5?'flat':p>0?'up':'down';return`<span class="v54-trend ${c}">${p>0?'+':''}${num(p,1)}%</span>`};
function stock54(){const cur=S.data?.current||{},list=[...(cur.jeddah?.items||[]).map(x=>({...x,branch:'jeddah',branchLabel:'جدة'})),...(cur.riyadh?.items||[]).map(x=>({...x,branch:'riyadh',branchLabel:'الرياض'}))];return{cur,list,total:Number(cur.jeddah?.totalQty||0)+Number(cur.riyadh?.totalQty||0),value:Number(cur.jeddah?.inventoryValue||0)+Number(cur.riyadh?.inventoryValue||0),zero:Number(cur.jeddah?.zeroSkuCount||0)+Number(cur.riyadh?.zeroSkuCount||0),low:Number(cur.jeddah?.lowStockSkuCount||0)+Number(cur.riyadh?.lowStockSkuCount||0),negative:Number(cur.jeddah?.negativeSkuCount||0)+Number(cur.riyadh?.negativeSkuCount||0),noPrice:Number(cur.jeddah?.noPriceSkuCount||0)+Number(cur.riyadh?.noPriceSkuCount||0)}}
function soldMap54(t){const m=new Map();for(const x of t.items.values())m.set(x.sku,x);return m}
function replenishment54(t,stock){if(!stock.list.length)return[];const sold=soldMap54(t),days=periodDays54(),m=new Map();for(const it of stock.list){const s=sold.get(it.sku),daily=Number(s?.qty||0)/Math.max(1,days),qty=Number(it.qty||0),cover=daily>0?qty/daily:9999,score=daily>0?(daily*10)/(Math.max(1,qty)):0,old=m.get(it.sku);const rec={sku:it.sku,name:it.name,qty:0,sales:Number(s?.qty||0),daily,cover,score,branches:[]};if(old){old.qty+=qty;old.cover=daily>0?old.qty/daily:9999;old.score=daily>0?(daily*10)/Math.max(1,old.qty):0;old.branches.push({label:it.branchLabel,qty})}else{rec.qty=qty;rec.branches=[{label:it.branchLabel,qty}];m.set(it.sku,rec)}}return[...m.values()].filter(x=>x.sales>0&&(x.cover<=14||x.qty<=0)).sort((a,b)=>b.score-a.score||a.cover-b.cover).slice(0,10)}
function slowStock54(t,stock){if(!stock.list.length)return[];const sold=soldMap54(t),m=new Map();for(const it of stock.list){const x=m.get(it.sku)||{sku:it.sku,name:it.name,qty:0,value:0};x.qty+=Math.max(0,Number(it.qty||0));x.value+=Math.max(0,Number(it.value||0));m.set(it.sku,x)}return[...m.values()].filter(x=>x.qty>0&&!sold.get(x.sku)?.qty).sort((a,b)=>b.value-a.value||b.qty-a.qty).slice(0,8)}
function stockHealth54(stock){return`<div class="v54-metric-list"><div class="v54-metric-row"><div class="v54-metric-main"><div class="v54-metric-title">قيمة المخزون التقديرية</div><div class="v54-metric-sub">وفق سعر البيع الحالي في ملف التسعير</div></div><div class="v54-metric-value">${stock.value?money(stock.value):'—'}</div></div><div class="v54-metric-row"><div class="v54-metric-main"><div class="v54-metric-title">أصناف بكمية صفر</div></div><span class="v54-badge ${stock.zero?'warn':'ok'}">${num(stock.zero)}</span></div><div class="v54-metric-row"><div class="v54-metric-main"><div class="v54-metric-title">أصناف منخفضة</div><div class="v54-metric-sub">عند أو دون حد الشد/الحد الأدنى التشغيلي</div></div><span class="v54-badge ${stock.low?'warn':'ok'}">${num(stock.low)}</span></div><div class="v54-metric-row"><div class="v54-metric-main"><div class="v54-metric-title">كميات سالبة</div></div><span class="v54-badge ${stock.negative?'bad':'ok'}">${num(stock.negative)}</span></div><div class="v54-metric-row"><div class="v54-metric-main"><div class="v54-metric-title">أصناف بدون سعر</div></div><span class="v54-badge ${stock.noPrice?'warn':'ok'}">${num(stock.noPrice)}</span></div></div>`}
const oldBranchCard=branchCard;branchCard=function(label,b,current,color){const extra=current?.inventoryValue?`<div class="branch-metric"><small>قيمة المخزون</small><b style="font-size:11px">${money(current.inventoryValue)}</b></div><div class="branch-metric"><small>مخزون منخفض</small><b>${num(current.lowStockSkuCount)}</b></div>`:'';return`<div class="branch-card"><div style="display:flex;align-items:center;gap:8px;margin-bottom:8px"><i style="width:9px;height:9px;border-radius:50%;background:${color}"></i><h3>${label}</h3></div><div class="branch-metrics"><div class="branch-metric"><small>مبيعات</small><b>${num(b.sales)}</b></div><div class="branch-metric"><small>قيمة المبيعات</small><b style="font-size:11px">${money(b.value)}</b></div><div class="branch-metric"><small>دخول مخزون</small><b>${num(b.inbound)}</b></div><div class="branch-metric"><small>تحويل مخزني</small><b>${num(b.transfer)}</b></div><div class="branch-metric"><small>المخزون الحالي</small><b>${num(current?.totalQty)}</b></div><div class="branch-metric"><small>أصناف متوفرة</small><b>${num(current?.positiveSkuCount)}</b></div>${extra}</div></div>`};
overview=function(t){const cur=S.data.current||{},prev=previous54(),stock=stock54(),days=periodDays54(),repl=replenishment54(t,stock),slow=slowStock54(t,stock),avgQty=t.sales/Math.max(1,days),avgValue=t.value/Math.max(1,days);return`<div class="v54-kpis"><div class="v54-kpi"><div class="v54-kpi-head"><span class="v54-kpi-label">الكمية المباعة</span>${badge54(t.sales,prev?.sales)}</div><div class="v54-kpi-value">${num(t.sales)}</div><div class="v54-kpi-sub">متوسط ${num(avgQty,1)} يوميًا</div></div><div class="v54-kpi"><div class="v54-kpi-head"><span class="v54-kpi-label">قيمة المبيعات</span>${badge54(t.value,prev?.value)}</div><div class="v54-kpi-value" style="font-size:21px">${money(t.value)}</div><div class="v54-kpi-sub">متوسط ${money(avgValue)} يوميًا</div></div><div class="v54-kpi"><div class="v54-kpi-head"><span class="v54-kpi-label">دخول المخزون</span>${badge54(t.inbound,prev?.inbound)}</div><div class="v54-kpi-value">${num(t.inbound)}</div><div class="v54-kpi-sub">توريد وزيادات مسجلة</div></div><div class="v54-kpi"><div class="v54-kpi-head"><span class="v54-kpi-label">التحويل المخزني</span><span class="v54-trend flat">مطابق</span></div><div class="v54-kpi-value">${num(t.transfer)}</div><div class="v54-kpi-sub">مستبعد من المبيعات</div></div><div class="v54-kpi"><div class="v54-kpi-head"><span class="v54-kpi-label">أصناف تحركت</span><span class="v54-trend flat">SKU</span></div><div class="v54-kpi-value">${num(t.changed.size)}</div><div class="v54-kpi-sub">بيع أو دخول أو تحويل</div></div><div class="v54-kpi"><div class="v54-kpi-head"><span class="v54-kpi-label">المخزون الحالي</span><span class="v54-trend flat">الفرعان</span></div><div class="v54-kpi-value">${num(stock.total)}</div><div class="v54-kpi-sub">إجمالي الوحدات المسجلة</div></div><div class="v54-kpi"><div class="v54-kpi-head"><span class="v54-kpi-label">وصل للصفر</span><span class="v54-trend ${t.zero?'down':'flat'}">خلال الفترة</span></div><div class="v54-kpi-value">${num(t.zero)}</div><div class="v54-kpi-sub">حركة تستحق مراجعة التوريد</div></div><div class="v54-kpi"><div class="v54-kpi-head"><span class="v54-kpi-label">مخزون منخفض حاليًا</span><span class="v54-trend ${stock.low?'down':'up'}">مباشر</span></div><div class="v54-kpi-value">${num(stock.low)}</div><div class="v54-kpi-sub">حسب حد الشد/5 وحدات عند غيابه</div></div></div>
<div class="v54-section-title"><div><h2>الفروع</h2><p>أداء ومخزون كل فرع بشكل مستقل.</p></div></div><div class="branch-cards">${branchCard('جدة',t.j,cur.jeddah,'#0f6b4f')}${branchCard('الرياض',t.r,cur.riyadh,'#296f92')}</div>
<div class="v54-grid wide" style="margin-top:12px"><section class="v54-card"><div class="v54-card-head"><div><h3>اتجاه المبيعات</h3><p>مقارنة جدة والرياض عبر تحديثات المخزون.</p></div></div><div class="v54-card-body">${trendChart(t)}</div></section><section class="v54-card"><div class="v54-card-head"><div><h3>صحة المخزون</h3><p>نقاط الخطر الحالية التي تؤثر على الاستمرارية.</p></div></div><div class="v54-card-body">${stockHealth54(stock)}</div></section></div>
<div class="v54-grid two" style="margin-top:12px"><section class="v54-card"><div class="v54-card-head"><div><h3>المبيعات مقابل دخول المخزون</h3><p>هل التوريد يغطي سرعة الخروج؟</p></div></div><div class="v54-card-body">${movementChart(t)}</div></section><section class="v54-card"><div class="v54-card-head"><div><h3>توزيع المبيعات بين الفرعين</h3><p>حصة كل فرع من الكمية المباعة.</p></div></div><div class="v54-card-body">${donut(t)}</div></section></div>
<div class="v54-grid two" style="margin-top:12px"><section class="v54-card"><div class="v54-card-head"><div><h3>أولوية إعادة التوريد</h3><p>أصناف تتحرك سريعًا ورصيدها الحالي قد لا يغطي أسبوعين.</p></div><span class="v54-badge ${repl.length?'warn':'ok'}">${repl.length}</span></div><div class="v54-card-body v54-metric-list">${repl.map((x,i)=>`<div class="v54-metric-row"><span class="v54-rank">${i+1}</span><div class="v54-metric-main"><div class="v54-metric-title">${esc(x.name||x.sku)}</div><div class="v54-metric-sub">${esc(x.sku)} · بيع ${num(x.sales)} · رصيد ${num(x.qty)}</div></div><div class="v54-metric-value">${x.cover<999?num(x.cover,1)+' يوم':'—'}</div></div>`).join('')||'<div class="v52-empty">لا توجد أولوية حرجة وفق البيانات الحالية.</div>'}</div></section><section class="v54-card"><div class="v54-card-head"><div><h3>مخزون بدون حركة بيع</h3><p>أعلى قيمة/كمية لم تسجل بيعًا خلال الفترة المختارة.</p></div></div><div class="v54-card-body v54-metric-list">${slow.map((x,i)=>`<div class="v54-metric-row"><span class="v54-rank">${i+1}</span><div class="v54-metric-main"><div class="v54-metric-title">${esc(x.name||x.sku)}</div><div class="v54-metric-sub">${esc(x.sku)} · رصيد ${num(x.qty)}</div></div><div class="v54-metric-value">${x.value?money(x.value):'—'}</div></div>`).join('')||'<div class="v52-empty">لا تتوفر بعد بيانات لقيمة المخزون/الحركة البطيئة.</div>'}</div></section></div>
<div class="v54-card" style="margin-top:12px"><div class="v54-card-head"><div><h3>الأصناف الأكثر مبيعًا</h3><p>أعلى الأصناف خروجًا من المخزون بعد استبعاد التحويل.</p></div></div><div class="v54-card-body">${topBars(t)}</div></div>`};
const baseRender54=render;render=function(){baseRender54();document.title='المبيعات والمخزون | الإدارة التنفيذية';};
if(S.data)render();window.__V54_SALES={version:VERSION,stock:stock54};
}catch(e){console.error('[V54 sales]',e)}
})();